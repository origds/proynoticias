<div class ="titulos" >
  <h1>Noticias Sin Publicar</h1>
</div>

<div class="span12" style="margin-top:20px;">

  <% if @reports.count == 0 %>
  No hay noticias por publicar.
  <% else %>

  <table style="width:800px">
    <thead>
      <tr>
        <th style="width:500px">Título</th>
        <th>Usuario</th>
        <th>Ver</th>
        <th>Eliminar</th>
      </tr>
    </thead>

    <tbody>
      <% @reports.each do |report| %>
      <tr>
        <td><%= report.title %></td>
        <td><%= User.find(report.user_id).email %></td>
        <td><%= link_to glyph(:eye_open), report %></td>
        <td><%= link_to glyph(:remove), report, method: :delete, data: { confirm: 'Esta seguro de eliminarla?'} %></td>
      </tr>
      <% end %>
      <% end %>
    </tbody>
  </table>
</div>